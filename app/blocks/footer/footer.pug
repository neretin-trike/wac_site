mixin footer()
    footer.footer
        .container.footer__container
            +logo("logo_white.png")
            +menu()(class="menu__link_inverse")
            +contact()(class="contact__list_inverse")
